{% extends "./base.html.twig" %}
{% block body %}
<section class="flex flex-col justify-center items-center pt-11">
        <b class="border-b-2 border-black p-4">Qui suis je ?</b>
        <h1 class="text-xl pt-2">Vladimir Boussekeyt</h1>
        <p >Motivé, curieux, souhaite pouvoir
            perfectionner ses techniques de
            programmations.</p>
        <p> Passionné par le cinéma, les livres.
            Activité sportive : pratique de la boxe</p>
        <p>Futurs projet : faire un robot pour discord</p>
    </section>
    <section class="flex flex-col justify-center items-center pt-6">
        <b class="border-b-2 border-black p-4 ">Mes réalisations : cliquez sur les images pour plus de détails</b>
        <div class="flex flex-row pt-6 gap-2">
            {% for projet in projets %}
            <div class="flex flex-col h-52 w-72">
                    {% if projet.imageSources|length > 0 %}
                        <a href={{"projet/"~loop.index}}><img src={{projet.imageSources[0]}} ></a>
                    <p>{{projet.name}}</p>
                {% endif %}
            </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}